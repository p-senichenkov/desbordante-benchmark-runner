[project]
name = "desbordante-benchmark-runner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Petr Senichenkov", email = "Petua41@yandex.ru" }
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.4.2",
    "matplotlib>=3.11.1",
    "pydantic>=2.13.4",
]

[project.scripts]
desbordante-benchmark-runner = "desbordante_benchmark_runner:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
